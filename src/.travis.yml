language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t kebab/docker-react -f Dockerfile.dev . 

script:
  - docker run -e CI=true kebab/docker-react npm run test -- --coverage
